{
  "scripts": {
    "build": "parcel build src/content.js --dist-dir dist --no-source-maps",
    "dev": "parcel src/content.js --dist-dir dist --open false"
  },
  "devDependencies": {
    "@parcel/config-default": "^2.16.0",
    "buffer": "^6.0.3",
    "parcel": "^2.16.0",
    "process": "^0.11.10"
  },
  "dependencies": {
    "@tensorflow-models/pose-detection": "^2.1.3",
    "@tensorflow/tfjs": "^4.22.0",
    "@tensorflow/tfjs-backend-webgl": "^4.22.0"
  }
}
